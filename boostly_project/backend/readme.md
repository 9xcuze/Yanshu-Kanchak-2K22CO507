# Boostly Backend (Skeleton)
This folder contains the SQLite migrations and a compact Spring Boot skeleton (source code).
Run migrations:
  sqlite3 boostly.db < migrations/000_schema.sql

You can run the Java backend (provided in the full project) after building with Maven.
